{
  "name": "reservation-deploy-listener",
  "version": "1.0.0",
  "description": "Simple webhook listener to trigger staging deploy for reservationRepo",
  "main": "listener.js",
  "scripts": {
    "start": "node listener.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
